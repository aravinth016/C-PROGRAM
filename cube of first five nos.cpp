#include<stdio.h>
#include<math.h>
int main()
{
    int n,x=3,i,p;
	for(n=1;n<=5;n++)
	{
		for(i=1;i<=1;i++)
		{
			p=pow(n,x);
			printf("num is %d and the cube of %d is %d\n",n,i,p);
		}
		}
		return 0;	
}
